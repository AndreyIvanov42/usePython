trigger:
- master

pool: macos

steps:
  - bash: env PYTHON_CONFIGURE_OPTS="--enable-framework" pyenv install 3.10.5
  - task: UsePythonVersion@0
    inputs:
      versionSpec: 3.10.5
  - bash: python --version
